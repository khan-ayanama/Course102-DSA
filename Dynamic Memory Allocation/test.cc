#include <iostream>
using namespace std;

// Creating Micro
#define PI 3.14
int main()
{
    // Macros

    // #define(preprocessor directive) --> creates macro
    // A peice of code in a program that is replaced by value of micro

    // Using Micro
    int radius = 5;
    double area = PI*radius*radius;
    cout<<"Area: "<<area<<endl;

    // Homework --> READ GFG MACRO ARTICLE

    // GLOBAL VARIABLE
    // Bad practice

    // Inline functions
    // They are used to reduce the function calls overhead

    // Const variable
}