#include <iostream>
using namespace std;
int main()
{
    // TIME AND SPACE COMPLEXITY PART-1

    // Time complexity --> IT is the amount of time taken by an algorithm to run as a funciton of length of the input

    // It is used to for making better programs or comparison of Algorithm

    // Big O notation ---> Upper Bound
    // Theta notation ----> Average complexity
    // Omega notation ----> Lower bouond

    // BIG O NOTATION
    // 1.Constant time ---> O(1)
    // 2.Linear time ----->O(n)
    // 3.Logarithm time ---->O(log n)
    // 4.Quadratic time ----> O(n^2)
    // 5.Cubic time ----> O(n^3)

    // COMPLEXITY ORDER IN INCREASING ORDER

    // O(1) < O(logN) < O(N) < O(NlogN) < O(N^2) < O(N^3) < O(2^n) < O(N!)

    // Calculating time complexity of some question

    // Highest complexity will take a lead
    // 1. 2n^2 + 3n  == O(n^2)
    // 2. 4n^4 + 3n^3 == O(n^4)
    // 3. N^2 + logN == O(N^2)
    // 4.1220  === O(1)
    // 5. n^3/300 == O(n^3)

    // 10^8 Rule ----> Most of the modern machines can perform 10^8 operation per seconds

    // #################    SPACE COMPLEXITY    ############
    // 1. O(1)
    // 2.O(n)
}